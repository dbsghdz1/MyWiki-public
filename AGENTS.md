# MyWiki 운영 스키마 (공개판)

이 저장소는 진행 중인 프로젝트를 중심으로 지식을 축적하는 개인용 LLM Wiki다. 사람은 원본을 선별하고 질문한다. 에이전트는 원본을 보존하면서 프로젝트 기록, 위키의 요약, 종합, 상호참조, 색인, 로그를 관리한다.

> [!note] 공개판 안내
> 원본 볼트는 비공개이며 금융·계획·구직·인프라 운영 영역을 함께 담고 있다. 이 문서는 공개 저장소에 실제로 포함된 영역(`학습/`, `작업노트/`, `프로젝트/개인/`, `_wiki/`)에 해당하는 규칙만 남긴 판본이다. 규칙 자체는 원본과 같다.

## 디렉터리 소유권

- `프로젝트/개인/`: 개인 프로젝트 영역이다. 각 프로젝트는 `프로젝트/개인/<이름>/`에 둔다.
- `학습/`: 학습 활동 전체의 상위 영역이다 (2026-08-30 재편). 하위 셋: `공부/`(이해 노트), `야생학습/`(실제 작업이 계기가 된 손으로 배우기 세션 기록), `아카이빙/`(비활성 노트 보관).
- `학습/공부/`: 사람(홍)이 다시 읽고 이해를 쌓는 1인칭 학습 노트 영역이다. `학습/공부/<분야>/<주제>.md`에 날짜 항목을 append하며 frontmatter는 `type: study`, `audience: me`. `_wiki/`와의 경계 — `_wiki`는 외부 출처를 종합한 지식(출처 링크 의무), 공부는 작업·학습 경험이 근거인 기록이다. 세부 규칙은 `학습/공부/README.md`를 따른다.
- `작업노트/`: AI 에이전트가 작업하며 알게 된 것을 다음 작업의 에이전트를 위해 남기는 경험 노트 영역이다 (2026-08-28 `학습/공부/`에서 분리). `작업노트/<분야>/<주제>.md`에 날짜 항목을 append하며 frontmatter는 `type: study`, `audience: ai`. API명·에러 메시지·커밋 해시 같은 검색 키를 요약해 지우지 않는다. 세부 규칙은 `작업노트/README.md`를 따른다.
- `_wiki/`: 에이전트가 관리하는 공용 위키다. 주제 허브, 종합 페이지, `index.md`가 포함된다.
  - `_wiki/Sources/`: 보존된 원본이다. 에이전트는 읽기만 한다. 한 번 수집된 파일의 내용, 이름, 경로를 수정하거나 삭제하지 않는다.
- `README.md`: 사람을 위한 빠른 시작 문서다.

영역 허브: `프로젝트/개인/README.md`, `학습/README.md`, `학습/공부/README.md`, `작업노트/README.md`는 `type: hub`인 영역 진입점이다. 하위 문서 목록과 항상 일치해야 하며, 프로젝트를 추가·이동·상태 변경하면 함께 갱신한다.

## 절대 규칙

1. 원본과 위키를 구분한다. 원본에서 발견되지 않은 내용을 사실처럼 쓰지 않는다.
2. 원본을 조용히 덮어쓰거나 정정하지 않는다. 잘못된 원본도 그대로 보존하고 위키에서 문제를 설명한다.
3. 검증 가능한 주장 묶음마다 문단 또는 표 행 단위로 원본 링크를 단다.
4. 서로 충돌하는 주장은 하나를 삭제하지 말고 모두 남긴 뒤 `근거 충돌` 경고 블록으로 표시한다.
5. 새 파일보다 기존 주제 허브의 갱신을 우선한다. 여러 출처를 함께 압축할 독립적인 주제가 생길 때만 새 페이지를 만든다.
6. `_wiki/index.md`는 현재 위키와 항상 일치해야 한다.
7. 작업 커밋에는 해당 작업에서 변경한 문서만 포함한다.

## 프로젝트 작업공간

각 프로젝트 폴더의 `README.md`가 그 프로젝트의 진입점이다. 상태, 문제 정의, 현재 범위, 주요 결정, 코드 저장소, 관련 위키와 원본을 연결한다.

- **코드 저장소 연동**: 프로젝트 README의 frontmatter `repos`와 본문 `코드 저장소` 섹션에 저장소를 기록한다. 에이전트가 프로젝트 질문을 받으면 실제 코드·커밋을 읽고 답한다.
- **작업 기록 축적**: 개발하며 생긴 결정·실험·일지는 프로젝트 폴더에 `<주제> YYYY-MM-DD.md` 파일로 쌓고 README에서 링크한다.
- 외부 자료와 보존해야 할 원문은 프로젝트 폴더에 복사하지 않고 `_wiki/Sources/`에 보존한다.
- 여러 프로젝트에서 재사용할 수 있는 지식은 `_wiki/`에 종합하고 프로젝트 README에서 링크한다.
- 프로젝트에만 해당하는 진행 상황을 일반 위키로 승격하지 않는다.
- **배운 것 연결**: 작업 중 새로 이해한 것은 성격에 따라 기록한다 — 사람이 이해할 개념·원리는 `학습/공부/<분야>/<주제>.md`, AI용 작업 경험은 `작업노트/<분야>/<주제>.md`. 어느 쪽이든 프로젝트 README의 `## 배운 것` 섹션과 해당 작업 기록에서 그 주제를 링크한다. 항목 쪽에도 맥락 프로젝트 링크가 있어야 한다.

프로젝트 README frontmatter:

```yaml
---
type: project
status: idea | active | paused | shipped | archived
aliases: []
created: YYYY-MM-DD
updated: YYYY-MM-DD
repos:
  - "<로컬 경로> — github.com/<owner>/<repo>"
related_wiki: []
---
```

## 문서 형식

위키 페이지 frontmatter:

```yaml
---
type: hub | synthesis
status: active | needs-review | disputed | stale
aliases: []
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources:
  - "[[_wiki/Sources/...]]"
---
```

- 본문은 한국어로 작성하고 널리 쓰이는 영문 기술 용어는 병기하거나 `aliases`에 넣는다.
- 사실과 해석을 구분한다. 출처가 직접 말하지 않은 종합은 `해석` 또는 `종합`임을 밝힌다.
- 버전, 가격, 성능, 제품 기능처럼 변하는 주장에는 `YYYY-MM-DD 기준`을 표시한다.
- 링크는 Obsidian wikilink를 쓴다. 표시명이 필요하면 `[[경로|표시명]]`.

충돌 표기:

```markdown
> [!warning] 근거 충돌
> - 주장 A — [[원본 A]]
> - 주장 B — [[원본 B]]
> - 현재 판단: 미해결 | 조건에 따라 다름 | 한쪽 근거가 더 강함
> - 검토일: YYYY-MM-DD
```

## Operation: Ingest

새 자료를 수집할 때의 순서다.

1. 수집함에서 처리 대상 파일을 찾는다.
2. 내용·제목·원 URL이 기존 Sources와 같은지 확인한다. 중복이면 이동·페이지·로그를 만들지 않고 알린다.
3. 자료를 `_wiki/Sources/YYYY/MM/`로 옮긴다. 날짜는 수집일. 이동 후 원본을 수정하지 않는다.
4. 관련 프로젝트 README, 위키 페이지, `_wiki/index.md`를 **먼저 읽는다.**
5. 출처별 요약 파일을 기계적으로 늘리지 않는다. 기존 허브를 갱신하고, 독립적인 다중 출처 주제일 때만 새 허브를 만든다.
6. 새 주장과 기존 주장의 충돌, 기준일 차이, 용어 차이를 확인한다.
7. `_wiki/index.md`를 갱신한다.
8. 변경된 링크, 출처, 고립 페이지, 영향받은 페이지의 drift를 경량 점검한다.

채팅으로 직접 받은 자료는 출처 유형을 `사용자 제공 원문` 또는 `사용자 제공 요약`으로 명시한다. 원문을 확인하지 않았다면 요약을 원문으로 표현하지 않는다.

## Operation: Sync (프로젝트 작업 연동)

"<프로젝트> 작업 정리해줘" 요청의 순서다.

1. 프로젝트 README의 `repos`에 적힌 저장소에서 최근 커밋·변경 파일을 읽는다.
2. 마지막 기록 이후의 작업을 `<주제> YYYY-MM-DD.md`로 프로젝트 폴더에 정리한다. 커밋 메시지 기반 해석은 해석임을 밝힌다.
3. 프로젝트 README의 현재 상태·다음 작업을 갱신한다.
4. 여러 프로젝트에 재사용 가능한 지식이 나오면 `_wiki/`의 관련 허브에 종합한다.
5. 새로 이해한 것이 있으면 성격에 따라 `학습/공부/<분야>/<주제>.md`(사람이 이해할 개념·원리) 또는 `작업노트/<분야>/<주제>.md`(AI용 작업 경험)에 append하고 양방향 링크를 건다. 없으면 건너뛴다 — 억지로 만들지 않는다.

## Operation: Query

1. `_wiki/index.md`를 읽고 관련 프로젝트·허브를 선택한다.
2. 위키에서 답을 구성하되 근거 확인이 필요할 때 Sources나 연동된 코드 저장소를 읽는다.
3. 답변에 근거가 되는 원본 링크를 포함한다.
4. 기존 위키의 단순 재진술이면 파일을 변경하지 않는다.
5. 여러 출처를 결합한 새 종합, 새 충돌, 기존 페이지의 명백한 개선만 위키에 자동 반영한다.

## Operation: Lint

매 수집에서는 영향 범위만 경량 점검한다. 마지막 전체 lint가 30일 이상 지났으면 다음 작업에서 전체 lint를 실행한다.

전체 lint 검사 항목:

- 끊어진 wikilink와 존재하지 않는 출처
- index 등재 대상(영역 허브·프로젝트 README·`_wiki/` 주제 허브)이 index에 빠져 있거나, index에서만 참조되는 페이지
- 다른 페이지와 연결되지 않은 고립 페이지
- 출처가 없는 검증 가능 주장
- 서로 충돌하지만 경고가 없는 주장
- 기준일이 오래된 제품·모델·버전 정보
- 같은 내용을 반복하는 페이지와 지나치게 잘게 나뉜 페이지
- frontmatter의 `updated`, `status`, `sources`와 본문의 불일치
- 프로젝트 README의 `repos` 경로가 실제로 존재하는지
- 영역 허브 README의 목록이 실제 하위 폴더·문서와 일치하는지
- 공부·작업노트 주제 파일의 `projects`·맥락 링크와 프로젝트 README `## 배운 것` 링크가 양방향으로 존재하는지, CS 주제 파일에 `## 참고 자료`가 있는지

자동으로 안전하게 고칠 수 있는 문제는 수정한다. 판단이 필요한 문제는 `needs-review`로 표시한다.

## Index

`_wiki/index.md`는 프로젝트와 위키 페이지의 링크, 한 줄 설명, 상태, 갱신일을 담는 카탈로그다. 질의할 때 가장 먼저 읽는다.

**등재 범위**: index에는 **영역 허브, 프로젝트 README, `_wiki/`의 주제 허브·종합 페이지**만 올린다. 날짜별 작업 기록(`<주제> YYYY-MM-DD.md`), 공부·작업노트 영역의 주제 파일, `_wiki/Sources/`는 **등재하지 않으며** 각자 소속된 허브에서 링크한다. 따라서 "index에 없는 페이지"는 그 자체로 결함이 아니다 — 결함은 **어느 허브에서도 링크되지 않은 경우**다.
