---
type: study
area: CS
audience: me
status: active
created: 2026-08-15
updated: 2026-08-18
projects:
  - "소프트웨어마에스트로"
---

# 데이터베이스

관계형 DB가 안에서 어떻게 동작하는가 — 인덱스(B+Tree), 트랜잭션과 격리 수준, 실행 계획, 정규화. 소마 백엔드(Spring Boot + RDB)의 쿼리·스키마 결정을 근거 있게 하는 것이 목표. 백엔드 경험을 풀스택으로 넓히는 전략(계획 우선순위)과 직결.

## 핵심 정리

(아직 없음 — 학습 후 B+Tree 인덱스가 빠른 이유·인덱스가 안 타는 경우, 격리 수준 4단계와 각 이상 현상, 실행 계획 읽는 법을 정리한다)

## 학습 계획

CS 학습 슬롯 중 **주 2회**(각 45~60분), W4~W11 (2026-08-16 2차 재편으로 주 1회→2회 확대, [[공부/README|공부]] 학습 계획). 이론(CMU) + 실무(Use The Index, Luke) 병행. 완료하면 체크하고 `## 기록`에 남긴다.

- [ ] W4 (2026-09-08 주): 인덱스 — PlanetScale "How do database indexes work?"로 감 잡기 → Use The Index, Luke Ch.1~2 (Anatomy of an Index, Where Clause) 정독
- [ ] W4: CMU 15-445 Storage·B+Tree 회차 시청. B+Tree가 빠른 이유·인덱스가 안 타는 경우 정리
- [ ] W5 (09-15): 트랜잭션 — PostgreSQL 문서 "Transaction Isolation" 정독, 4개 격리 수준과 dirty/non-repeatable/phantom read 표
- [ ] W5: CMU 15-445 Concurrency Control 회차
- [ ] W6 (09-22): 실행 계획 — PostgreSQL EXPLAIN 장. 소마 DB 테이블 하나 골라 실제 인덱스·`EXPLAIN` 확인
- [ ] W6: SQLBolt로 조인·서브쿼리·집계 실습 (익숙하면 스킵)
- [ ] W7 (09-29): 정규화·조인 전략(Nested Loop / Hash / Merge)
- [ ] W7: DDIA Ch.3 (Storage and Retrieval) — LSM 트리 vs B-Tree
- [ ] W8 (10-06): 락·MVCC — PostgreSQL이 격리 수준을 어떻게 구현하는지
- [ ] W8: 소마 영수증/필요서류 API의 쿼리·스키마 결정 하나를 근거와 함께 `## 기록`에
- [ ] W9 (10-13): 복제·파티셔닝 개론 — DDIA Ch.5~6
- [ ] W9: 커넥션 풀·N+1·트랜잭션 범위 — Spring Boot 관점으로 소마 코드 대조
- [ ] W10 (10-20): `## 핵심 정리` 작성 (2세션)
- [ ] W11 (10-27): 면접 예상 질문으로 핵심 정리 검증 (1세션)
- [ ] W11: 소마 DB 실제 쿼리 2~3개를 EXPLAIN으로 다시 읽고 `## 기록`에 한 줄씩 (1세션)

## 기록

### 2026-08-16 — MySQL의 UNIQUE는 NULL 중복을 막지 못한다

- 맥락: 보험찾개냥 SSH-310 룰 테이블. `document_rule`에 보험사별 룰과 **폴백 룰**(모르는 보험사용)을 같은 테이블에 두면서, 폴백은 `insurance_company_code`를 NULL로 뒀다.
- 배운 것:
  - `UNIQUE(insurance_company_code, claim_type)`를 걸어도 **폴백 행은 몇 개든 들어간다.** MySQL의 UNIQUE 인덱스는 NULL을 서로 다른 값으로 취급하기 때문이다 — SQL에서 `NULL = NULL`이 참이 아닌 것의 연장이다.
  - 우회는 **생성 컬럼(generated column)에 UNIQUE를 거는 것**이다. `company_key VARCHAR(30) AS (IFNULL(insurance_company_code,'*')) STORED`처럼 NULL을 실제 값으로 치환한 컬럼을 만들고 거기에 제약을 건다. `STORED`여야 인덱스를 걸 수 있다.
  - 즉 **"값이 없음"을 제약의 대상으로 삼으려면 없음을 값으로 만들어야 한다.**
- 근거: `Server/src/main/resources/db/migration/V2__create_document_rule.sql`, 커밋 `8957a3e`. `DocumentRuleSeedTest`가 중복 INSERT에 `DuplicateKeyException`이 나는지로 확인한다.

### 2026-08-18 — CHECK 제약으로 "코드에만 있는 규칙"을 DB에 내리기

- 맥락: 같은 프로젝트 SSH-293. `claim_type`·`provided_by`·`requirement`가 애플리케이션에선 enum인데 DB에선 그냥 `VARCHAR`였다. 운영자가 SQL로 오타 섞인 값을 넣으면 조회가 터지는 구조였다.
- 배운 것:
  - MySQL은 `CHECK` 제약을 **INSERT·UPDATE·REPLACE·LOAD DATA에서 평가**하고 FALSE면 에러를 낸다. `NOT ENFORCED`로 만들어 두는 것도 가능하니, 있다고 다 강제되는 건 아니다.
  - `ALTER TABLE ... ADD CONSTRAINT ck_x CHECK (col IN ('A','B'))` 형태로 **이미 데이터가 있는 테이블에도 나중에 걸 수 있다.** 기존 행이 위반하면 ALTER가 실패하므로, 성공했다는 것 자체가 기존 데이터 검증이 된다.
  - 대가는 **값을 늘릴 때 마이그레이션이 필요해진다**는 것이다. 이건 부작용이 아니라 노리는 바다 — "코드(enum)와 DB가 같이 바뀌어야 한다"를 스키마로 못박는 것.
  - 방어를 **읽을 때가 아니라 쓸 때** 두는 판단은 [[공부/백엔드/JPA와 Spring Data|JPA와 Spring Data]]에 적었다.
- 근거: `V3__add_document_rule_enum_check.sql`, 커밋 `0cdeacf`. 로컬·CI 양쪽에서 기존 43룰·276항목에 걸리는 행 없이 적용됐다.

## 참고 자료

- [CS 지식의 정석 — 큰돌, 인프런](https://www.inflearn.com/course/%EA%B0%9C%EB%B0%9C%EC%9E%90-%EB%A9%B4%EC%A0%91-cs-%ED%8A%B9%EA%B0%95) — 네트워크 파트를 W1~W3에 듣는 같은 강의의 섹션 5 데이터베이스(3시간 59분). W4~ 계획의 **보조 자료 후보**(주 교재는 아래) — 면접형 "Q." 강의를 핵심 정리 검증에 쓸 수 있다 (2026-08-18 확인)
- [CMU 15-445/645 Intro to Database Systems](https://15445.courses.cs.cmu.edu/) — DB 내부 구조 강의의 표준(Andy Pavlo). 강의 영상·슬라이드 공개 (2026-08-15 확인)
- [Use The Index, Luke](https://use-the-index-luke.com/) — 개발자를 위한 SQL 인덱싱 무료 온라인 책. 실무 인덱스 지식의 정석 (2026-08-15 확인)
- [PostgreSQL Docs — Transaction Isolation](https://www.postgresql.org/docs/current/transaction-iso.html) — 격리 수준을 가장 명확하게 설명한 공식 문서 (2026-08-15 확인)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/current/) — 레퍼런스. 인덱스 유형·EXPLAIN 장 (2026-08-15 확인)
- [Designing Data-Intensive Applications (Kleppmann)](https://dataintensive.net/) — 저장·복제·트랜잭션의 원리를 다루는 현대 표준서. 책 소개 사이트 (2026-08-15 확인)
- [How do Database Indexes Work? — PlanetScale](https://planetscale.com/blog/how-do-database-indexes-work) — 인덱스 개념을 O(N)→O(log N)으로 직관적으로 설명 (2026-08-15 확인)
- [MySQL 8.4 — CREATE INDEX (Unique Indexes)](https://dev.mysql.com/doc/refman/8.4/en/create-index.html) — "A UNIQUE index permits multiple NULL values for columns that can contain NULL" 원문 (2026-08-18 확인)
- [MySQL 8.4 — CHECK Constraints](https://dev.mysql.com/doc/refman/8.4/en/create-table-check-constraints.html) — 평가 시점(INSERT·UPDATE·REPLACE·LOAD DATA)과 `[NOT] ENFORCED` (2026-08-18 확인)
- [SQLBolt](https://sqlbolt.com/) — 브라우저에서 바로 하는 SQL 실습 (2026-08-15 확인)
