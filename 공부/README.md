---
type: hub
status: active
aliases: [학습 노트, TIL, study]
created: 2026-08-15
updated: 2026-08-21
---

# 공부

프로젝트·운영을 하면서 **내가 새로 이해하게 된 것**을 주제별로 쌓는 영역 허브다. `_wiki/`가 외부 출처(Sources)를 종합한 지식이라면, 공부는 실제 작업 경험에서 나온 1인칭 학습 노트다. CS든 프레임워크든 도구든, 배운 것은 전부 여기로 모인다.

## 원칙

1. **주제별 파일, 날짜별 항목 append.** 같은 주제로 배운 게 쌓이며 한 파일이 두꺼워지는 것이 목적이다. 날짜별 파일은 만들지 않는다.
2. **양방향 연결.** 공부 항목은 맥락이 된 프로젝트를 링크하고, 그 프로젝트 README의 `## 배운 것` 섹션은 공부 주제를 링크한다. 작업 기록 파일(`<주제> YYYY-MM-DD.md`)에도 `배운 것:` 한 줄로 공부 주제를 링크한다. 한쪽만 있으면 미완성이다.
3. **작업에 닿아 있어야 한다.** 근거는 커밋·파일·재현한 현상·실제 결정이다. 작업과 무관한 일반론을 옮겨 적지 않는다.
4. **CS 주제는 유명 레퍼런스를 붙인다.** 자료구조·알고리즘, OS, 네트워크, DB, 언어 런타임·컴파일러, 분산 시스템, 보안 등 CS 주제 파일에는 `## 참고 자료`에 널리 인정되는 자료(교과서, 공식 문서, RFC, 대학 강의, 널리 인용되는 글) 2~4개를 **URL을 실제로 확인한 뒤** 첨부하고 `YYYY-MM-DD 확인`을 표기한다. 확인 못 한 링크는 넣지 않는다.
5. **`_wiki` 승격.** 한 주제가 외부 출처 여럿을 종합할 만큼 커지면 `_wiki/`에 주제 허브를 만들고 공부 파일에서 링크한다. 공부 파일은 그대로 남긴다.

## 파일 규칙

- 경로: **`공부/<분야>/<주제>.md`** (2026-08-21 폴더 도입). 분야는 `CS` · `JS` · `Apple` · `AppStore` · `백엔드` · `도구` · `개발방법` 일곱이고, **아래 「주제 목록」의 절 이름과 1:1로 맞춘다.** 주제명은 검색 가능한 명사구 (예: `Swift 동시성.md`, `HTTP 캐싱.md`).
- **어느 분야인지 애매하면 폴더를 새로 만들지 말고 [[공부/미분류|미분류]]에 항목만 append한다.** 같은 성격이 3개 모이면 그때 분야를 만들고, 만들면 주제 목록에 절을 함께 추가한다.
- frontmatter:

```yaml
---
type: study
area: CS | JS | Apple | AppStore | 백엔드 | 도구 | 개발방법
status: active | stale
created: YYYY-MM-DD
updated: YYYY-MM-DD
projects:
  - "[[프로젝트/개인/<이름>/README|<이름>]]"
---
```

- 본문 구조:

```markdown
# <주제>

한 줄 요약 — 현재 시점의 내 이해.

## 핵심 정리
누적된 이해를 갱신하는 살아있는 요약. 기록이 쌓이면 여기를 고쳐 쓴다.

## 기록
### YYYY-MM-DD — 제목
- 맥락: [[프로젝트/개인/<이름>/README|<이름>]]에서 무엇을 하다가
- 배운 것: 결론 먼저 1~3불릿
- 근거: 커밋 / 파일 / 재현 / 외부 링크

## 참고 자료
- [제목](URL) — 왜 볼 만한지 한 줄 (YYYY-MM-DD 확인)
```

- **계획 중인 주제**는 `## 학습 계획` 섹션에 웹 학습 체크리스트(주차·자료·범위·완료 기준)를 둔다. 완료한 항목은 체크하고 배운 것을 `## 기록`으로 옮긴다.
- [[공부/미분류|공부/미분류.md]]: 주제가 아직 애매한 항목을 임시로 두는 곳. "공부 미분류 정리해줘"로 주제 파일에 옮기고 원 항목은 삭제한다 (append-only 아님).
- 프로젝트 README의 `## 배운 것` 섹션: 첫 기록 시 만든다. `- [[공부/<주제>|<주제>]] — 이 프로젝트에서 배운 요점 한 줄` 형식의 목록.

## 주제 목록

허브 규칙에 따라 실제 파일과 항상 일치시킨다. **절 이름은 `공부/` 하위 폴더와 1:1로 맞춘다.** 각 줄은 `링크 — 한 줄 요약 (갱신일)`.

### CS
- [[공부/CS/OSI 7계층|OSI 7계층]] — **↔ TCP/IP 4계층 대응표 완성**(44강). 차이는 셋뿐 — 애플리케이션이 셋으로·링크가 둘로 갈라지고 인터넷 계층을 네트워크 계층이라 부른다 (2026-08-21)
- [[공부/CS/네트워크|네트워크]] — CS 강의 38~99강을 W1~W3 15세션에 배분. **W1-1·W1-2 완료(~44강)** — 캡슐화가 계층화를 성립시킨다, PDU 이름은 계층별로 다르다, 체크섬은 1의 보수합(순환 캐리)·CRC-32로 계층마다 다르다 (2026-08-21)
- [[공부/CS/자료구조와 알고리즘|자료구조와 알고리즘]] — 코딩테스트 대비. W4(09-08)부터 **주 1회** 유형 하나씩 (2026-08-16 재조정)
- [[공부/CS/데이터베이스|데이터베이스]] — 인덱스·트랜잭션·실행 계획, 소마 백엔드 직결. W4~W11 **주 2회**로 확대 (2026-08-16 재조정)
- [[공부/CS/운영체제|운영체제]] — 프로세스·스케줄링·가상 메모리·동시성·파일시스템, OSTEP 기반. W4~W11 주 1회 (2026-08-16)

### JS (웹·프론트엔드)
- [[공부/JS/JavaScript 모듈 시스템|JavaScript 모듈 시스템]] — default/named export, `export { default as X } from` 재수출, 접근제어자 없음 (2026-08-16)
- [[공부/JS/JavaScript 기초 문법|JavaScript 기초 문법]] — 객체 리터럴·구조 분해·truthy/falsy, Swift에 없는 것만 골라서 (2026-08-18)
- [[공부/JS/JavaScript 런타임|JavaScript 런타임]] — Node = V8 + OS 기능, 브라우저와 능력이 다른 이유 (2026-08-18)
- [[공부/JS/TypeScript 타입 시스템|TypeScript 타입 시스템]] — 타입은 컴파일하면 사라진다, 외부 데이터는 손으로 검증 (2026-08-18)
- [[공부/JS/Next.js 서버와 캐싱|Next.js 서버와 캐싱]] — 서버 컴포넌트, fetch revalidate, Route Handler (2026-08-18)
- [[공부/JS/Feature-Sliced Design|Feature-Sliced Design]] — 계층·슬라이스·세그먼트, index.ts public API, Next.js `_app`/`_pages`, Steiger (2026-08-20)
- [[공부/JS/외부 API 데이터 모델링|외부 API 데이터 모델링]] — 응답 타입 두 겹, 숫자로 들고 다니기, 메타데이터 하드코딩의 함정 (2026-08-20)

### Apple (Swift·iOS·macOS)
- [[공부/Apple/SwiftUI|SwiftUI]] — `.plain` 버튼 히트 영역은 라벨 불투명 픽셀뿐, `.blur`는 사방 가장자리, 가변 폭 메뉴는 `fixedSize`, 알파만 있는 어두운 토큰은 다크에서 사라짐 (2026-08-18)
- [[공부/Apple/Swift와 Objective-C 브리징|Swift와 Objective-C 브리징]] — `@objc` 셀렉터는 Swift 이름+인자 레이블에서 생성(`mouseEntered(with:)` → `mouseEnteredWith:`), 프레임워크 콜백과 어긋나면 조용히 미호출. `responds(to:)`·`strings`로 검증 (2026-08-18)
- [[공부/Apple/WidgetKit과 AppIntents|WidgetKit과 AppIntents]] — AppIntents 문구는 컴파일 타임 리터럴만 허용, 런타임 L10n과 공존 불가 → xcstrings + knownRegions. `AppIntentConfiguration`은 macOS 14+ (2026-08-19)
- [[공부/Apple/SceneKit과 3D 에셋 파이프라인|SceneKit과 3D 에셋 파이프라인]] — **다시 export하는 것은 생김새를 바꾸는 일**. 숨긴 오브젝트는 변환이 평가 안 됨, usdz 애니는 이미 씬 타임베이스, 가시성 애니는 못 믿음, 조각된 셰이프 키 ≫ 노드 배율 (2026-08-20)
- [[공부/Apple/macOS 메뉴바와 샌드박스|macOS 메뉴바와 샌드박스]] — 샌드박스는 AX 전면 차단이지만 **IORegistry 조회는 그대로 허용**(배터리 사이클 수), 메뉴바 아이템 식별은 CGWindowList + 화면 기록 권한(재실행 필요) (2026-08-19)
- [[공부/Apple/Xcode 빌드와 번들 구성|Xcode 빌드와 번들 구성]] — 동기화 폴더에서 파일 빼기는 pbxproj `membershipExceptions`(예외=제외), 기능을 끄면 **릴리즈 최적화가 문자열째 걷어내므로** `strings` 대조가 검증이 된다 (2026-08-20)

### AppStore (스토어 운영)
- [[공부/AppStore/App Store 성장 도구|App Store 성장 도구]] — **Mac 전용 앱은 Apple Ads·인앱 이벤트·커스텀 제품 페이지가 전부 막힌다**(전부 iPhone/iPad 전용). 남는 것은 피처링 노미네이션(최소 3주 전)·프로모션 텍스트 (2026-08-19)
- [[공부/AppStore/App Store Server Notifications|App Store Server Notifications]] — 비소모성도 프로덕션 ONE_TIME_CHARGE 수신, 실패 시 1h→12h→24h→48h→72h 재시도(72h 안에 고치면 자동 도착), 전송 이력·테스트는 App Store Server API(ASC 팀 키로 가능) (2026-08-19)

### 백엔드
- [[공부/백엔드/JPA와 Spring Data|JPA와 Spring Data]] — 매핑 실패는 행이 아니라 조회 전체를 죽인다, `DataAccessException` 번역은 완전하지 않다 (2026-08-18)

### 도구·인프라
- [[공부/도구/Vercel 배포|Vercel 배포]] — Git 연동은 레포 루트 기준, 서브폴더 사이트는 Root Directory 필수(빠지면 통째로 404), 배포 URL은 보호 때문에 401/302라 alias로 점검 (2026-08-19)
- [[공부/도구/문서 포맷 파싱|문서 포맷 파싱]] — `.pptx`는 zip+OOXML, `.key`는 zip+IWA(snappy 압축 protobuf) — 앱 없이 텍스트 추출 가능. PDF 메타데이터는 날짜 추정 근거 (2026-08-19)
- [[공부/도구/Claude Code 설정과 훅|Claude Code 설정과 훅]] — **설정이 안 먹으면 문법보다 로딩 시점을 의심한다.** `paths: "**/*"`는 항상 로드가 아니라 조건부(빼야 세션 시작 로드), path-scoped는 Read에만, `allowed-tools`는 제한이 아니라 사전승인, 복합 명령은 조각별 매칭, 새 `settings.json`은 그 세션에서 안 먹음 (2026-08-21)

### 개발 방법
- [[공부/개발방법/테스트 코드|테스트 코드]] — 초록인데 아무것도 안 지키는 단언들, Testcontainers 컨테이너 수명·Docker 의존(커밋 게이트에 못 넣는 이유), mock과 stub·「가짜로 바꾸는 선」 (2026-08-21)
- [[공부/개발방법/오픈소스 기여|오픈소스 기여]] — 당근 SEED Design(`daangn/seed-design`) 코드베이스 분석·기여. **시작을 2026-09-22(W6)로 연기** — 사전 조사만 끝난 상태 (2026-08-20)

## 학습 계획 (2026-08-15 수립 → 08-16 두 차례 재편 → 08-17 강의 기반 교체 → **08-21 JS 우선으로 재편**)

**슬롯 세 개.** 하루 총량은 **약 1시간 15분**이며 08-20에 되돌린 "하루 1시간" 기조를 크게 넘기지 않는다.

| 슬롯 | 시간 | 내용 |
|---|---|---|
| **JS 문법 워밍업** | **매일 15분**, 프로젝트 착수 **전** | 아래 6일 순환. 파일 만들지 말고 `node` REPL에서 손으로. [[공부/JS/JavaScript 기초 문법\|JavaScript 기초 문법]] |
| **주간 복습** | 일요일 30분 | 그 주 커밋 diff를 열어 **한 줄씩 소리 내어 설명**. 막히는 줄이 다음 주 워밍업 주제 |
| ~~오픈소스 기여~~ | **2026-09-22(W6)부터** | [[공부/개발방법/오픈소스 기여\|오픈소스 기여]] — 08-20 결정대로 연기 유지 |

> [!important] 2026-08-21 — JS 문법을 CS보다 위에 올렸다 (본인 결정)
> **진단**: MyCryptoDiary D1이 계획 6.5h → 4 작업일이 걸렸는데, 막힌 지점을 세어 보니 전부 **JS 언어 자체**였다 — 구조 분해(`const { searchParams } = ...`), 객체 리터럴(`{ error: '...' }`), truthy/falsy(`if (!markets)`), 배열 메서드(`join`/`split`/`find`). React·Next·FSD 같은 상위 개념은 오히려 빨리 이해했다(캐시 주기를 데이터 변화 속도로 정하기, 메타데이터 하드코딩을 스스로 버린 판단). 상세는 [[프로젝트/개인/MyCryptoDiary/모의투자 전환 D1 2026-08-16|D1 작업 기록]].
> **언어가 안 익으면 모든 게 3배 어려워진다** — 코드를 읽을 때 문법 해독에 뇌를 다 써서 정작 설계를 볼 여유가 없다.
> **방법은 강의·책이 아니라 손**이다. 근거: 튜토리얼 없이 4일 만에 위젯을 실데이터로 연결했고 그 와중에 설계 판단까지 했다. 반대로 대화 중 길게 설명한 개념은 지금 다시 물으면 절반이 흐릿하다.
> **우선순위 근거**: 지원 D-day 2026-11-14에 직접 걸리는 것은 **포트폴리오(이력서 1번 항목)**이고, JS는 그 속도를 직접 올린다. CS는 **면접 대비**이고 면접은 지원 이후라 11-14가 마감이 아니다.

### JS 문법 워밍업 — 6일 순환 (매일 15분)

`node` 띄우고 **예측 먼저, 그다음 엔터.** 15분 넘기지 않는다. 틀린 것이 그날 배운 것.

| 일 | 주제 | 예 |
|---|---|---|
| 1 | 객체 리터럴 | 만들기, 점/대괄호 접근, 추가·삭제, 참조 비교 |
| 2 | 구조 분해 | 객체·배열, 이름 바꾸기, 기본값, rest, 함수 파라미터 |
| 3 | 배열 메서드 | `map` `filter` `find` `join` `split` `reduce` |
| 4 | truthy/falsy·연산자 | falsy 6개, `?.`, `??`, 삼항, `===` |
| 5 | 비동기 | `Promise`, `async/await`, `.then` `.catch` `Promise.all` |
| 6 | 타입 | `type` 정의, 유니온, 옵셔널, `as`가 왜 위험한지 |

한 바퀴 돌면 다시 1일로. 막히지 않는 주제는 건너뛰고 약한 것만 반복한다.

### 작업 중 규칙 — 막히면 그 자리에서 파지 않는다

D1에서 가장 큰 시간 손실은 개념 질문이 **작업 흐름을 끊은 것**이었다(`client.ts` 쓰다가 "Node가 뭐야" 20분, "포트가 뭐야" 20분). 이해는 됐지만 진도 체감이 0이 됐다.

→ **작업 중에는 "이렇게 쓰면 됨"만 알고 넘어가고, 궁금한 것은 메모만 한다.** 그날 작업이 끝난 뒤 몰아서 설명·기록한다. (에이전트가 메모를 들고 있다가 마무리 때 정리한다.)

### CS 학습 — 주 2회로 축소

| 주차 | 주간 파일 | 배분 (2슬롯) | 이번 주 목표 |
|---|---|---|---|
| 1 | 주간 2026-08-18 | 네트워크 2 | ✅ 38~44강 (W1-1·W1-2 완료) |
| 2 | 주간 2026-08-25 | 네트워크 2 | 45~52강 — MTU/MSS·계층별 역할·3/4-웨이 핸드셰이크·라우팅 |
| 3 | 주간 2026-09-01 | 네트워크 2 | 53~57강 — IP/MAC/ARP·IPv4/6·클래스풀/리스·서브네팅 |
| 4 | 주간 2026-09-08 | 네트워크 2 | 58~63강 — NAT·HTTP 헤더/버전·HTTPS/TLS |
| 5 | 주간 2026-09-15 | 네트워크 2 | 64~73강 — 브라우저 저장소·세션/토큰 인증 |
| 6 | 주간 2026-09-22 | 네트워크 2 | 74~86강 — 상태코드·메서드·장치·LAN |
| 7 | 주간 2026-09-29 | 네트워크 2 | 87~99강 + **`## 핵심 정리` 완성** |
| 8 | 주간 2026-10-06 | DB 2 | 인덱스 — PlanetScale + Use The Index Luke Ch.1~2 |
| 9 | 주간 2026-10-13 | DB 2 | 트랜잭션·격리 수준 — PostgreSQL 문서 + CMU |
| 10 | 주간 2026-10-20 | DB 1 + OS 1 | 실행계획 EXPLAIN / 프로세스·스레드(OSTEP Ch.4~6) |
| 11 | 주간 2026-10-27 | DB 1 + OS 1 | 락·MVCC / 가상 메모리(OSTEP Ch.13~16) |
| 12~13 | 주간 2026-11-03 ~ 11-10 | 면접 답변 정리 2 | 공고 마감 주간. **새 개념을 넣지 않는다.** 핵심 정리를 면접 답변 형태로 다듬는다 |

**요일 배분 기본값 (일간 루틴이 따른다)**: **화 · 금**에 CS 슬롯, 나머지 요일은 JS 워밍업만. 일요일에 주간 복습.

> [!warning] 이 재편이 버리는 것
> **코딩테스트 대비를 표에서 뺐다.** 08-16 2차 재편에서 주 1회로 줄였던 것을 이번엔 0으로 만든 것이다. 근거는 **코딩테스트가 있더라도 지원(11-14) 이후 전형**이라 11-14 전에 준비할 필요가 없다는 것. 다만 08-16 경고("실제로 코테가 관문이면 통과선을 못 만든다")는 여전히 유효하며, **2027 윈터테크 공고에서 전형 절차를 확인하는 즉시 이 판단을 다시 본다.**
> **OS·DB도 얇아졌다** — DB 6세션, OS 2세션. 면접에서 깊게 물으면 부족하다. 11-14 이후 면접 준비 기간에 이어서 채운다는 전제다.
> **되돌림 조건**: JS 워밍업을 2주 돌린 뒤 프로젝트에서 문법 때문에 막히는 빈도가 눈에 띄게 줄면, CS를 주 3회로 되돌린다. 반대로 워밍업조차 소화가 안 되면 CS를 주 1회로 더 줄이고 워밍업만 남긴다.

## 자주 쓰는 요청

- `이거 공부에 기록해줘` — 지금 대화의 배운 것을 주제 파일에 append + 프로젝트 README `배운 것` 링크
- `<프로젝트> 작업 정리해줘` — Sync 중 배운 것을 자동 추출해 공부에 반영
- `공부 미분류 정리해줘` — 미분류 항목을 주제 파일로 이동
- `<주제> 레퍼런스 찾아줘` — CS 주제의 참고 자료 보강
